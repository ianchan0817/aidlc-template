---
description: TypeScript/JavaScript strictness — no `any`, explicit public return types, interface vs type, strict mode, no unguarded `!`, camelCase/PascalCase/SCREAMING_SNAKE_CASE/kebab-case. Attaches to .ts/.tsx/.js/.jsx/.mjs/.cjs only; the language-neutral half is the code-style rule.
paths:
  - "**/*.{ts,tsx,js,jsx,mjs,cjs}"
---

Apply the `## TypeScript` section of `aidlc/rules/code-style.md`.
